<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.store.kh.GeneralStore.mapper.PizzaMapper">

    <select id="selectAll">
        select * from PizzaMenu
    </select>

    <select id="selectById">
        select *
        from PizzaMenu
        where id=#{id}
    </select>

    <delete id="deletePizza">
        delete
        from PizzaMenu
        WHERE id=#{id}
    </delete>

    <insert id="insertPizza">
        <!-- id 값은 mysql 에서 값이추가될 경우 자동 증가 설정 -->
        insert into PizzaMenu
        values name = #{name}, price = #{price}, description = #{description}, image_path = #{imagePath}
    </insert>

    <update id="updatePizza">
        update PizzaMenu
        SET
            <!-- SQL 컬럼명 = DTO 변수명 -->
            name = #{name},
            price = #{price},
            description = #{description},
            image_path = #{imagePath}
        WHERE id = #{id}
    </update>

    <insert id="insertPizza">
        insert into PizzaMenu(name, price, description, image_path)
        values (#{name}, #{price}, #{description}, #{imagePath})
    </insert>
</mapper>